{
  "name": "domain",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "clean": "rimraf ./lib | rimraf tsconfig.tsbuildinfo",
    "build:watch": "yarn clean | tsc -b tsconfig.json -w",
    "build": "tsc -b tsconfig.json"
  },
  "packageManager": "yarn@3.2.1",
  "devDependencies": {
    "ts-node": "^10.8.1",
    "tslint": "^6.1.3",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "rimraf": "^3.0.2"
  }
}
